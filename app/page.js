export default function Home() {
  return <h1>Andrepogi</h1> // Don't use this as entry point,
  //  just create a button leading to progresio/login (entry point for actual system)
}

{
  /** Karl do this:
  
  no need to use next auth, just custom jwt like the usual
  Background: #FFFFFF (pure white)
Surface / Cards: #F8FAFC (very light gray)
Border / Divider: #E5E7EB
Primary Text: #0F172A (almost-black navy)
Secondary Text: #475569

  */
}
